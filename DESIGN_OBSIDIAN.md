# 设计：Obsidian 对接（远期）

> 状态：**设计，未实现**。远期任务，排在 Session Export Package 之后。
> 上位文件：[ROADMAP.md](ROADMAP.md)

## 1. 你手上实际有三类材料

| 材料 | 谁来处理 | 难度 | 现状 |
|---|---|---|---|
| **上课录音** | 本项目（到 REPAIRED）+ 网页会话 | 最难 | 已跑通 |
| **课件** | 直接丢网页会话 | 低 | 手工，可用 |
| **课本** | 直接丢网页会话 | 低 | 手工，可用 |

录音之所以最难，是因为它**必须先变成文本**才能被处理，而这一步（Whisper、
选择性重转录、时间轴、课程归档）没有别的工具替你做。课件和课本本来就是文本或
图像，网页会话能直接读 —— 这就是为什么它们不需要流水线。

**这个不对称决定了整个 Obsidian 设计：录音要有工程，课件课本不要有工程。**

## 2. 核心判断：Obsidian 不需要程序化写入

Phase 4 原来的设想是程序自动往 Vault 里写笔记、建概念页、建课程索引、建知识图谱。
按 A/B 对照得到的同一个教训，这里要提前说清楚：

**知识库的价值来自你自己的整理动作，不来自自动生成的页面数量。**

自动建出来的概念页和 WikiLink，如果你从来不打开，它们和 A 组那 245 个
`[!question]` 是同一类东西 —— 看起来很完整，实际增加噪声。

所以设计原则改为：

> **程序负责把材料准备到「可以直接存进 Vault」的状态，存不存、怎么连，由你决定。**

## 3. 分层：三类材料怎么进库

```text
                   ┌─────────────────────────────────────┐
录音 ──本项目──► REPAIRED ──┐                            │
                            ├─► 一次 GPT 网页会话 ─► 笔记 ─┼─► Obsidian Vault
课件 ──手工───────────────┤                            │
课本 ──手工───────────────┘                            │
                   └─────────────────────────────────────┘
```

三类材料在**同一个网页会话里**汇合，产出一份笔记。这正是 Session Export Package
要解决的事 —— 所以 Export Package 是 Obsidian 对接的前置，不是并列任务。

## 4. Vault 目录结构（建议，不强制）

```text
<Vault>/
  University/
    数字电子技术基础/
      课堂笔记/
        2026-09-01_0943_第01次课.md        ← 一节课一篇，来自网页会话
      课件/
        第1章-数制和码制.pdf
      教材/
        教材批注.md                          ← 你自己读课本时写的
      _索引.md                              ← 手写，不自动生成
  _附件/
    2026-09-01_0943_board_01.jpg           ← 板书照片
```

- `obsidian_folder` 已经在 `config/courses.yaml` 里按课程配好了，直接复用。
- **一节课一篇笔记**，文件名带日期+时间+次序，和 session 命名规则一致。
- 概念页、索引、图谱**手写**。想连就连，不想连就不连。

## 5. 程序做什么（很少）

只做两件事，都是「搬运 + 命名」，不是「生成」：

### 5.1 `lecture-ai vault-import <session_id> --note <网页会话产出的 md>`

- 校验这份笔记确实对应该 session（问一次，不猜）；
- 补 front-matter：`course` / `date` / `session_id` / `source_layer` / `duration`；
- 按 `courses.yaml` 的 `obsidian_folder` 放到正确目录，用规范文件名；
- 板书照片复制进 `_附件/`，并把笔记里的引用改成 Obsidian 图片链接；
- 在 session 的 `metadata.json` 里记一笔「已入库 + 目标路径 + SHA」，避免重复导入。

### 5.2 `lecture-ai vault-status`

- 列出哪些 session 已入库、哪些还没有；
- 不做别的。

## 6. 明确不做

- **不自动建概念页。** `obsidian.create_concepts` 保持存在但默认改为 `false`，
  只在 High Integrity Mode 下由你显式开启。
- **不自动建 WikiLink。** 已有的「渲染器不得生成 WikiLink」硬闸门保留。
- **不自动建课程索引、不建知识图谱、不做 tag 推断。**
- **不做双向同步。** Vault 是终点，不回读、不覆盖 session 产物。
- **不处理课本全文。** 课本是版权材料且体量大，只支持你自己写的批注笔记进库。

## 7. 为什么把 `create_concepts` 默认关掉

A/B 对照里最有说服力的一条数据：A 组 245 个疑点块中，201 条覆盖的是低价值转录噪声，
却漏掉了唯一一处会影响备考的实质推断（把「毕业考试」听成「闭卷考试」）。

自动生成的概念页会重复同一个错误模式：**产出很多条目，但密度低、命中率低、
维护成本转嫁给你**。默认关掉，等你明确说「我需要概念页」再开。

## 8. 顺序

1. Session Export Package（近期，见 `DESIGN_EXPORT_PACKAGE.md`）
2. 用它跑几周真实课堂，确认默认路线稳定
3. 再做 `vault-import` —— 到那时你已经知道自己真正想要什么样的笔记结构了

**不要在第 2 步之前实现第 3 步。** 现在设计的 Vault 结构大概率会在实际用几周之后
改一遍，先写代码等于先写返工。
